# Запуск
```
./build/couse_work
./build/course_work example/test1.sql
```

# Просмотр логов

./build/course_logs 

# Запуск сервера
```
./build/course_server 8080
```
# Проверка сервера
```
curl http://127.0.0.1:8080/health
```
# Получение токена

curl -X POST http://127.0.0.1:8080/login \
  -H 'Content-Type: application/json' \
  --data-binary '{"user":"user","password":"user"}'

# Отправка SQL через клиент

./build/course_client 127.0.0.1 8080 example/test1.sql admin admin

# Отправка SQL через curl (консоль)

TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJhZG1pbiIsInJvbGUiOiJhZG1pbiIsImV4cCI6MTc4MDExMDMwM3"

curl -X POST http://127.0.0.1:8080/query \
  -H "Authorization: Bearer $TOKEN" \
  --data-binary 'USE demo; SELECT * FROM users;'

# Просмотр telemetry

curl http://127.0.0.1:8080/metrics

# Просмотр heartbeat

curl http://127.0.0.1:8080/heartbeat

